import { ListService, PagedResultDto } from '@abp/ng.core';
import { eFeatureManagementComponents } from '@abp/ng.feature-management';
import { GetTenantsInput, TenantDto, TenantService } from '@abp/ng.tenant-management/proxy';
import { ConfirmationService, ToasterService } from '@abp/ng.theme.shared';
import { Injector, OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import * as i0 from "@angular/core";
export declare class TenantsComponent implements OnInit {
    readonly list: ListService<GetTenantsInput>;
    private injector;
    private confirmationService;
    private service;
    private toasterService;
    private fb;
    data: PagedResultDto<TenantDto>;
    selected: TenantDto;
    tenantForm: FormGroup;
    isModalVisible: boolean;
    visibleFeatures: boolean;
    providerKey: string;
    modalBusy: boolean;
    featureManagementKey: eFeatureManagementComponents;
    get hasSelectedTenant(): boolean;
    onVisibleFeaturesChange: (value: boolean) => void;
    constructor(list: ListService<GetTenantsInput>, injector: Injector, confirmationService: ConfirmationService, service: TenantService, toasterService: ToasterService, fb: FormBuilder);
    ngOnInit(): void;
    private createTenantForm;
    addTenant(): void;
    editTenant(id: string): void;
    save(): void;
    delete(id: string, name: string): void;
    hookToQuery(): void;
    onSharedDatabaseChange(value: boolean): void;
    openFeaturesModal(providerKey: string): void;
    sort(data: any): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<TenantsComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<TenantsComponent, "abp-tenants", never, {}, {}, never, never>;
}
